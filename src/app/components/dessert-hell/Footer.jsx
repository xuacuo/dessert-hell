"use client";

export default function Footer() {
  return (
    <footer className="max-w-6xl mx-auto px-5 pb-10 text-xs text-zinc-500 text-center">
      Data: TheMealDB (public API). Features: API fetch, filters, modal, motion, SFX with settings,
      allergen icons, narrative strip, boss vignette, persistence, Halloween-styled chart, and
      poster header with skull cutout & torn-paper shadow. No &lt;section&gt; tags used.
    </footer>
  );
}
